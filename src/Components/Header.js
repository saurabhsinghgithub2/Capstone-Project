import React from 'react'
import logo from '../Images/logo.png'

const Header = () => {
    return (
        <header>
            <img src={logo} alt="" />
        </header>
    )
}

export default Header